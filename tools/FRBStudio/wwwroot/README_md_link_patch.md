# Markdownリンクボタン追加パッチ

## 配置

`index.html` を以下へ上書きしてください。

```text
F:\FRB\tools\FRBStudio_App\wwwroot\index.html
```

## 追加内容

ヘッダー右側に `Markdown Viewer / Editor` へのリンクボタンを追加しました。

リンク先：

```text
mdViewer.html
```

同じ `wwwroot` 配下に `mdViewer.html` がある前提です。
