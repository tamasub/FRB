# FX Studio / HSI Simulation Rule v0.15 Draft

更新日: 2026-07-12  
位置づけ: FX Studio / HSI Lab のシミュレーション仮説ルール  
ステータス: Draft / 検証前提

---

## 0. 大前提

これは売買推奨ルールではない。

```text
過去チャート上で
自分が何を見ていたのかを再現し、
Entry / Hold / Close / Reset / NoTrade を
自動シミュレーション可能な形へ分解する
```

ための仮説仕様である。

v0.15では、v0.14のEntry / Close条件を維持しつつ、NORMAL Rule Laneの語彙境界を明確化する。

```text
確定済みの押し安値 / 戻り高値
↓
直前構造高値 / 安値をM5確定足で突破
↓
M5 Dow Confirmation Event成立
↓
その時点ですでにR2以上なら即Entry
↓
実Entry地点より先の次HSI境界で全Close
```

このv0.15の定義は、v0.14のEntry / Close条件を引き継ぎ、NORMAL Rule Laneの語彙とLifecycleを上書きする。

## 0.1 Rule Laneごとの語彙境界

```text
NORMAL Rule Lane
- Entry
- Hold
- CloseOK
- CloseMiss
- Reset / Wait / Missed / Expired

NORMAL Rule Laneでは使わない
- ReEntry
- Add-on
- Expansion Core
- Expansion Detection Anchor
- Expansion Entry Anchor
```

NORMAL Rule Laneでは、過去に通常Tradeが完了しているかどうかに関係なく、新しいM5 Dow Confirmation Eventから成立したTradeはすべて独立した **Normal Entry** と呼ぶ。

```text
Normal Entry #1
→ Close
→ 新しいM5 Dow Confirmation Event
→ Normal Entry #2
→ Close
```

これは `ReEntry` ではない。回数識別が必要な場合は `normal_entry_sequence_no` を使用する。

```text
1 Dow Confirmation Event
= 最大1 Normal Entry Opportunity
= 最大1 Normal Entry
```

前回Trade終了後の次のNormal Entryには、新しいM5 Dow Confirmation Eventが必要である。ただし、この制約をReEntry条件とは呼ばない。

`ReEntry` と `Add-on` は、今後定義するExpansion / Expansion-Lite Rule Laneの専用語として予約する。NORMAL Rule Laneの判定・表示・集計・Traceへ混ぜてはならない。

---

## 1. v0.14の変更理由

v0.13では、M5 Dow Confirmation EventをHigh / Low比較ペアが完成した時点で発行していた。

しかし実際に人間が見ていたDow Up確定は、単にHigher Lowが確認された時点ではない。

```text
押し安値が確定
＋
その後に直前高値を上抜く
＝ Dow Up確定
```

Dow Downは対称形である。

```text
戻り高値が確定
＋
その後に直前安値を下抜く
＝ Dow Down確定
```

したがって、通常Entryの切符となるDow Confirmation Eventは、Swing比較ペアの完成時ではなく、**構造高値 / 安値の突破時**に発行する。

---

## 2. 時間足の役割

```text
WEEK = Expansion Season / 高値づかみ防止
DAY  = Expansion格付け / 上位地図
H4   = T3による通常方向許可 / Cycle Guard / Expansion判断
H1   = T3による通常方向確認 / Cycle Guard / Expansion観測
M5   = Dow Confirmation Event / Entry / Closeの実行
```

通常M5 Entryの必須要素は次のとおり。

```text
H4 T3方向
＋ H4 CloseのT3上下位置
＋ H1 T3方向
＋ H1 CloseのT3上下位置
＋ H4 / H1 Cycle Late除外
＋ M5 Dow Confirmation Event
＋ 確定HSI起点からR2以上
```

H4 Dow、H1 Dow、WEEK、DAY、Expansion Confirmedは通常M5 Entryの必須条件にしない。

---

## 3. M5 Dow StateとDow Confirmation Eventを分ける

### 3.1 M5 Dow State

```text
UP
DOWN
REVERSAL_WATCH
NO_TREND
UNDETERMINED
```

現在の方向状態を表す。

### 3.2 M5 Dow Confirmation Event

新しい構造が確定した出来事を表す。

```text
1 Dow Confirmation Event
= 1 dow_confirmation_id
= 1 HSI起点
= 最大1回の通常Entry機会
```

同じ `Dow State = UP` が続いていても、新しいDow Confirmation Eventがなければ次のNormal Entry Opportunityは発生しない。

---

## 4. Dow Up Confirmation

Long側の確認順序。

```text
① previous Lowがある
② その後に直前構造Highが形成される
③ さらにHigher Lowが確定する
④ ④以降のM5確定足が、②の構造Highを上抜く
⑤ そのM5確定足でDow Up Confirmation Event成立
```

HSI起点は、Higher Lowそのものではない。

```text
Normal HSI Anchor = previous Low
```

つまり、押し安値へ起点を乗り換えるのではなく、今回のDow波が始まった比較ペア先頭のLowを採用する。

---

## 5. Dow Down Confirmation

Short側は対称形。

```text
① previous Highがある
② その後に直前構造Lowが形成される
③ さらにLower Highが確定する
④ ④以降のM5確定足が、②の構造Lowを下抜く
⑤ そのM5確定足でDow Down Confirmation Event成立
```

```text
Normal HSI Anchor = previous High
```

---

## 6. 通常HSI起点Lifecycle

通常LaneとExpansion Laneを混ぜない。

### 通常Lane

```text
新しいM5 Dow突破確認
→ previous Low / previous Highを通常HSI起点へ採用

REVERSAL_WATCH / NO_TREND / UNDETERMINED
→ 通常HSI起点を解除

同方向へ再度Dow突破確認
→ 新しいDow Confirmation Event
→ 新しいprevious Swingを通常HSI起点へ採用
```

### Expansion Lane

```text
Expansion Detection Anchor
→ REVERSAL_WATCH等でも大起点を維持可能
→ 反対方向Dow成立まで保持
```

ただしExpansion Detection Anchorを、Normal EntryのHSI起点へ流用してはならない。

---

## 7. 通常Entryの判定順

### 7.1 Dow確認時点でR2未到達

```text
M5 Dow Confirmation Event成立
↓
固定HSI起点からR2未到達
↓
Entry Opportunity = WAITING_R2
↓
確認後のR2初回到達
↓
全Gate成立ならR2境界価格でEntry
```

R2初回到達時にGate不成立なら、そのOpportunityは `MISSED` とする。

### 7.2 Dow確認時点ですでにR2以上

```text
M5 Dow Confirmation Event成立
↓
固定HSI起点からR2以上
↓
Dow確認成立を最後のTriggerとして即Entry
```

これは遅れEntryではない。

```text
HSI距離条件は先に成立済み
＋
最後に不足していたDow確認が成立
＝ 即Entry
```

Entry価格はR2境界へ遡らない。

```text
Entry Price = Dow突破確認M5確定足の利用可能価格
```

初期実装では確定足Closeを使用する。

---

## 8. 通常Target

v0.13の「通常Targetは常にR2.5」を廃止する。

v0.14では、実Entry地点より先に残っている**次のHSI境界**をTargetとして固定する。

```text
Entry距離がR2〜R2.5
→ Target R2.5

Entry距離がR2.5〜R3
→ Target R3

Entry距離がR3〜R3.5
→ Target R3.5

Entry距離がR3.5〜R4
→ Target R4
```

同様に以後の境界へ続く。

Entry時にTargetを一つだけ固定し、到達時は10単位を全Closeする。

```text
1 Entry ID
= 1 Position
= 1 Stop
= 1 Target
= 1回の全Close
```

Partial Close、Runner、通常Add-onは使用しない。

---

## 9. Stop

```text
Stop Price = Entryに使用した通常HSI起点
```

Entry後に新しいSwingが確定しても、既存PositionのStopは移動しない。

Target / Stop到達判定はM5 High / Lowを使用する。

同一M5足でTargetとStopの両方へ到達した場合は、保守的にStopを先に適用する。

---

## 10. ReEntry

Close直後に同じDow状態を再利用して即ReEntryしない。

```text
Target到達 / StopClose
↓
そのTrade終了
↓
同じdow_confirmation_idは使用済み
↓
新しいM5 Dow Confirmation Eventを待つ
```

通常ReEntryの最低条件。

```text
前TradeのTerminal Close後
＋
新しいM5 Dow突破確認Event成立
＋
新しいdow_confirmation_id
＋
新しい通常HSI起点
```

新しいDow確認時点ですでにR2以上なら、その確認価格でReEntryする。

```text
新しいDow確認
→ R3.5〜R4地点で成立
→ その場でReEntry
→ Target R4
```

これは、同じ確認IDを使ってR2.5 / R3 / R3.5へ階段状に入り直す処理とは異なる。

### 禁止する処理

```text
R2 Entry → R2.5 Close
→ 同じDow確認IDで即R2.5 Entry
→ R3 Close
→ 同じDow確認IDで即R3 Entry
```

### 許可する処理

```text
R2 Entry → R2.5 Close
→ 新しい押し構造
→ 新しい直前高値突破
→ 新しいDow Confirmation Event
→ 現在地点からReEntry
→ 次HSI境界Close
```

---

## 11. Entry Opportunity Lifecycle

```text
WAITING_R2
= Dow確認済み。確認時点でR2未到達。

USED
= Entry実行済み。同じ確認IDは再利用禁止。

MISSED
= Entry判定時にGate不成立、または次Targetなし。

EXPIRED
= 表示範囲開始前の確認、前Trade Close以前の確認など。
```

1つの `dow_confirmation_id` は最大1つのOpportunityしか所有しない。

---

## 12. Entry Gate

通常Entry / ReEntryは次の全条件を満たす必要がある。

### Long

```text
H4 T3上向き
AND H4 Close > H4 T3
AND H1 T3上向き
AND H1 Close > H1 T3
AND H4 Cycle != Late
AND H1 Cycle != Late
AND M5 Dow Up Confirmation Event
AND Confirmation方向とEntry方向が一致
AND Confirmation採用HSI起点が解決済み
AND 固定起点からR2以上
AND 通常PositionがFlat
```

### Short

```text
H4 T3下向き
AND H4 Close < H4 T3
AND H1 T3下向き
AND H1 Close < H1 T3
AND H4 Cycle != Late
AND H1 Cycle != Late
AND M5 Dow Down Confirmation Event
AND Confirmation方向とEntry方向が一致
AND Confirmation採用HSI起点が解決済み
AND 固定起点からR2以上
AND 通常PositionがFlat
```

---

## 13. 図㉘の期待値

今回の観測例。

```text
通常HSI起点
= 2025-10-30 06:10 Low
= 152.164

直前構造高値
= 2025-10-30 06:30 High
= 153.135

Higher Low
= 2025-10-30 07:25 Low
= 152.560

Dow Up突破確認足
= 2025-10-30 09:40
High  = 153.513
Close = 153.498
```

09:40のM5確定足が構造高値153.135を上抜いた時点でDow Up確定。

```text
HSI起点 152.164
Entry価格 153.498
Entry距離 raw ≒ 222.3
```

これはR3.5〜R4帯にある。

```text
Dow確認時点でR2以上
→ 153.498で即Entry

次HSI境界
→ R4
→ Target 153.562
```

したがって、図㉘では通常Entryが発生しなければならない。

---

## 14. Expansionとの責務分離

通常EntryとExpansion Entryを混ぜない。

```text
通常Entry
= 新しいM5 Dow突破確認
+ 通常HSI起点
+ R2以上
+ 次HSI境界Target

Expansion Entry
= WEEK Season
+ DAY Grade
+ H4 Structure / BB / Cycle
+ H1 Permission
+ M5 Trigger
+ Expansion Detection Anchor
+ Expansion Entry Anchor
```

Expansionのみ二重起点を持ち得る。

```text
大起点
= Expansion Detection / 大波Target用

小起点
= Expansion Entry / Stop用
```

Expansion Core / Expansion Add-onの実行条件は未確定・未実装のままとする。

---

## 15. Traceへ残す項目

Dow Confirmation Event:

```text
confirmation_id
confirmation_time
confirmation_direction
previous High / current High
previous Low / current Low
breakout threshold price
breakout M5 bar OHLC
adopted HSI anchor
```

Entry Event:

```text
entry_execution_mode
entry_price
entry_distance_raw
entry_band_label
target_label
target_price
stop_price
dow_confirmation_id
entry_opportunity_id
```

Close Event:

```text
close_class
close_price
reached_target_label
realized_profit_jpy
run cumulative realized profit
```

Entryが発生しなかった場合も、最終的に止めたGateをreason_codeへ残す。

---

## 16. 現時点の禁止事項

```text
通常Add-on
Partial Close
Runner
同一Confirmation IDの再利用
Close直後の無条件ReEntry
未確定SwingをHSI起点へ使用
Expansion Detection Anchorを通常Entryへ流用
R2未到達での通常Entry
H4 / H1 Cycle Lateでの新規通常Entry
```

---

## 17. 検証観点

1. M5 Dow確認がSwing比較ペア完成時ではなく、構造突破時に発生するか。
2. 図㉘で09:40にDow Up Confirmation Eventが発生するか。
3. 09:40時点でR2以上として即Entryするか。
4. Entry地点の次境界R4をTargetへ固定するか。
5. 同じConfirmation IDをClose後に再利用しないか。
6. 新しいDow突破確認がTrade終了後に発生した場合だけReEntryできるか。
7. Normal AnchorとExpansion Detection Anchorが混線しないか。
8. 反対方向ケースでも対称に動くか。

---

## 18. Revision History

### 2026-07-12 v0.14

- M5 Dow Confirmation Eventを、High / Low比較ペア完成時から、確定済み押し安値 / 戻り高値後の構造高値 / 安値突破時へ変更。
- Dow確認時点でR2以上なら、R2.5通過済みでも即Entryを許可。
- 通常Targetを固定R2.5から、実Entry地点より先の次HSI境界へ変更。
- 1 Confirmation ID = 最大1 Entryを維持。
- 同じ確認IDによる階段ReEntryは禁止を維持。
- ReEntryには前Trade終了後の新しいM5 Dow突破確認Eventを必須とする。
- 図㉘の期待値を具体例として追加。

### 2026-07-12 v0.13

- Dow確認時点でR2到達状態を先に評価する方式へ変更。
- ただしDow確認時点とTargetが固定R2.5だったため、図㉘のケースを正しく扱えなかった。

---

## 最終要約

```text
Dow Stateは方向。
Dow Confirmation Eventは新しいEntryの切符。

押し安値 / 戻り高値が確定し、
直前構造高値 / 安値をM5確定足で突破した時点で、
新しいDow Confirmation Eventを発行する。

その時点でR2以上なら即Entry。
Targetは実Entry地点より先の次HSI境界。

同じ確認IDは1回しか使わない。
ReEntryには、前Trade終了後の新しいDow突破確認が必要。

通常は単一起点。
Expansionだけ二重起点。
```


## 15. NORMAL Rule Laneの実装契約（v0.15追加）

```text
Entry Evaluator Action
= ENTRYのみ

Close Evaluator Action
= FULL_CLOSE / STOP_CLOSE / WAIT

禁止Action
= REENTRY / ADD_ON
```

通常Entry Eventはすべて次の形式で記録する。

```json
{
  "rule_lane": "NORMAL",
  "event_type": "entry",
  "execution": {
    "action": "ENTRY",
    "normal_entry_sequence_no": 1
  }
}
```

Run集計では、NORMAL Laneの全Entryを `Entry` に合算する。`ReEntry` 件数へ含めない。

Close判定はEntry Rule Laneに従い、NORMAL Entryから生まれた建玉は必ずNormal Close Evaluatorで評価する。Expansion / Expansion-LiteのClose条件を参照しない。

---

## Revision History

- v0.15: NORMAL Rule LaneからReEntry / Add-on概念を除外。新しいDow確認ごとの独立TradeをすべてNormal Entryとして定義。Rule Lane別Action語彙と表示・集計契約を追加。
- v0.14: M5構造高値 / 安値の突破時にDow Confirmation Eventを確定し、R2到達済みなら即Entry。実Entry地点より先の次HSI境界をTargetとする。
